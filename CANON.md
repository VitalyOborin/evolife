# Канон EvoLife

Документ модели (2026-09-14). Не описание текущего кода фазы 0 и не спецификация реализации.
Симуляцию этот файл сам по себе не меняет. Числа — пакет: крутить только вместе.

Рядом: `IDEA.md` (цель), `README.md` (состояние кода).

---

## 1. Вопрос

Может ли в минимальной экосистеме (еда, цена хода, размножение, мутации) возникнуть
нервная память, если настоящее не содержит будущего?

Не «научиться делать X». Не усложнить топологию ради топологии.
Сложность мозга должна окупаться отбором. Иначе отбор прав, оставляя Брайтенберга.

**Формулировка успеха.** Адаптивный интеллект появился, когда линия стабильно решает
задачу, неразрешимую марковским тропизмом, и это решение размножается.

---

## 2. Жёсткие запреты

- Нет fitness-функции, LLM, backprop, RL, датасетов, человеческих меток.
- Нет GPS, компаса, id патча, запаха плодородия `H`.
- Еда и размножение автоматические (не моторы мозга).
- Основатель — чистый feedforward: 3 запаха → 0 скрытых → 2 мотора.
- Recurrence только через структурную мутацию (`add_node` / `add_connection`).
- В каноне энергия **не** в сенсорах и **не** в `tropism *= f(E)`.
- Сытость / сенсор энергии — отдельный **контрольный рукав**, не этот мир.
- Не дарить: `leave_mode`, заднюю камеру, датчик родства, собственность диска,
  наказание чужака, «не ешь», mating pool, NEAT-виды как отбор, Horde of Fame,
  два пола, рост `CHILD_DISPERSAL` «ради пола».

---

## 3. Канон мира

Цель мира — немарковость: **два мира с одинаковым запахом сейчас требуют разных действий.**

### 3.1 Поле

- Тор 512×512.
- ~9 дисков еды, радиус 36, ёмкость 40 частиц.
- Центры с джиттером (ровная решётка тора дарит обход прямой баллистикой).
- Ребро–ребро ~90; мёртвая зона шире ореола запаха (24).
- Частица вне диска — никогда.
- Глобального `FOOD_TARGET` нет; отрастание только локальное.
- Основатели рождаются внутри диска.
- Патчи стартуют с разным `n` и `H`, без общего пульса.
- Старт популяции на диск урезать (200 основателей / 9 дисков — сразу давка);
  глобальный `POPULATION_CAP` — только предохранитель, не лимит ниши.

### 3.2 Состояние диска

У каждого диска два числа:

| Символ | Видимо запахом? | Смысл |
|--------|-----------------|--------|
| `n` | да | число частиц |
| `H ∈ [0, 1]` | нет | скрытое плодородие |

Каждый тик:

- `H += 0.004 · (1 − H)`
- спавн частицы с вероятностью `0.08 · H`, пока `n < 40`
- съели частицу → `n -= 1`, `H -= 0.03`

Пустой диск, `(0,0,0)`:

- при `H ≈ 1` ждать ~12 тиков выгоднее ухода;
- при `H ≈ 0.1` ждать ~125 тиков — уходить;
- переход ~45 тиков, ~11 энергии на текущих константах хода
  (`MAX_LINEAR_SPEED = 2`, idle 0.05, move 0.10·скорость, порция 25).

Ждать vs уходить **не в сенсорах**. Пакет чисел:
`0.004 / 0.08 / 0.03 / 36 / 40 / щель ~90 / запах 24 / проба 8`.
Щель или стоимость хода поднять так, чтобы второй промах был фатален.

### 3.3 Сенсоры и тело

- Три пробы на 8 единиц вперёд, ±60°. Спины нет.
- Ореол запаха 24 (хвост частиц за краем диска).
- Моторы: поворот и ход (оба tanh).
- Телесный `heading` — уже интегратор. «Идти прямо» мозгу не заказывать:
  bias `turn ≈ 0`, `loco > 0` пересекает пустыню без recurrence.
- GPS / компас / проприоцепцию поворота в каноне ступени 1 **не** давать.

### 3.4 Кромка (режим ухода)

- Лицом в пустыню почти всегда `(0,0,0)` — тело несёт курс.
- Конфликт на **рыске**: боковой wisp своего ореола неотличим от струйки нового патча.
- Режим ухода — не флаг и не мотор, а короткий гистерезис ~12 тиков
  (запрет тропизма, пока не кончится хвост ореола). Recurrence τ ≈ 20.
- Без рыска баллистика и так уходит — тогда кромка почти ничего не требует от мозга.
- Сенсор сытости закрывает кромку без памяти (наелся → баллистика) и ломает
  wait-на-высоком-`H`. Поэтому в каноне сытости в мозге нет.

### 3.5 Возврат (главный спрос на recurrence)

Знание часов *этого* диска: после выедания снова кормит через ~150–400 тиков
(`H_RECOVERY = 0.004`). Желудок (одна порция) это не меряет.

**Ступень 1 (канон):** вышел → медленный интегратор τ ~ 1/0.004 → разворот π →
тот же телесный курс несёт назад. Карты нет.

**Позже (не канон сейчас):** path integration, trapline A→B→C→A, проприоцепция.

Нейрон-часы дешёвый (~1.3 энергии / 250 тиков при `NEURON_METABOLIC_COST = 0.005`).
Если не всплывёт — мало давления или редкий `add_node`, не «дорого».
Тогда ужимать удачу: сильнее джиттер, меньше патчей, дороже промах — **не** дарить карту.

### 3.6 Что ворует задачу у мира

Запах `H`, дождь частиц в пустыне, общий таймер патчей, `leave_mode`,
сытость в политике, компас, задняя камера, хищник-маяк, яркая стигмергия,
ровная решётка без джиттера, слишком дешёвый промах.

---

## 4. Канон наследственности

### 4.1 Единицы

| Роль | Без пола | С факультативным полом |
|------|----------|-------------------------|
| Интерактор | особь | особь |
| Репликатор | **весь геном** | **ген** (связь / узел по innovation) |

Без кроссовера часы и тропизм не имеют своей судьбы: клон копирует узлы, веса и
шрамы `add_node` одним куском. Возврат — сборка разных генов (тропзм 3×2,
порог ухода, hidden-часы, считывание на π). `add_node = 0.002` даёт деталь
в одной линии. Иначе клональная интерференция и Muller’s ratchet.

Пол по innovation разлепляет репликаторы. Без миграции на чужой диск —
инбридинг, геном снова камень.

**Не репликаторы:** вид NEAT, диск, `founder_lineage_id` (это счётчик).

### 4.2 Вязкость и диск

- Дети спавнятся в 1–4 единицах; при радиусе диска 36 **не покидают натальный диск**.
- Вязкость почти полная: сдержанность по `H` окупается своими детьми.
  Это не group selection. Датчик родства не дарить.
- Диск — трагедия общин: `H` общий, награда частная.
- Сдержанность = уйти при ещё ненулевом запахе (еду отменить нельзя).
  Часто это порог тропизма в весах, не цикл. Память — если уход по *истории*.
- Один на диске: сдержанность окупается только вместе с возвратом
  (пара: порог ухода × часы).
- Чужак = мигрант через пустыню: нужен полу (смешение линий) и крадёт `H`.
- Два режима сами, без видов: резиденты берегут диск без рекомбинации;
  бегуны смешивают гены и жгут общину.
- Мало бегунов → инбридинг; много → трагедия, возвращаться некуда.

### 4.3 Контактная рекомбинация (факультативный пол)

- Условие: энергия ≥ 48 (как порог размножения сейчас).
- Ближайший другой живой в радиусе 4 (как `EAT_RADIUS`), тоже ≥ 48.
- Есть партнёр → один ребёнок-кроссовер; нет → клон.
- Оба платят по 12 (сумма 24). Полов нет. ≤ 1 рождение / особь / тик.
- Родители свои геномы не меняют. После сборки — те же мутации, что у клона.
- Радиус секса = радиус еды, не шире (иначе секс через пустыню убивает вязкость).
- Не увеличивать `CHILD_DISPERSAL` «чтобы помочь полу» — украдёт спрос у мира.

**Как режется геном:**

- связи выравниваются по innovation (гомология);
- совпавшие: вес и `enabled` от случайного родителя, **не среднее**
  (среднее убивает выключенные шрамы `add_node`);
- несовпавшие: каждый ген независимо с p = 0.5, **не** от «фитнес-родителя»;
- узлы — объединение тех, кого взяли связи; bias — от родителя, у кого был
  (монетка, если у обоих).

Не делать: глобальный mating pool, выбор по дистанции генома, два пола,
Horde, disabled всегда от «лучшего».

---

## 5. Критерии адаптивного интеллекта

Не «есть hidden», не IQ, не размер мозга.
Поведение, которое нельзя свести к запаху **этого** тика, и которое размножается.

### 5.1 Ступени

| Ступень | Что | Это интеллект? |
|---------|-----|----------------|
| 0 | Популяция жива, ест, рожает | Нет — жизнеспособность фазы 0 |
| 1 | Разное действие при том же `o_t`, и оно окупается | Да — нервная память / контекст |
| 2 | То же, но тропизм и часы с разных дисков (сборка через пол) | Да — эволюция решений, не удачный клон |

Без ступени 1 цикл в геноме ничего не доказывает.

Примеры ступени 1:

- `(0,0,0)` после сильного запаха → ждать (~12); после выедания → уходить;
- боковой wisp на кромке → не повернуть (гистерезис ореола ~12);
- `(0,0,0)` спустя 150–400 тиков после ухода → разворот π.

### 5.2 Archive — поведение, не топология

| Наблюдение | Окно | Не путать с |
|------------|------|-------------|
| Вышел за ореол, не вернулся ≤ 12 тиков | кромка | баллистика без рыска |
| Съел в i → был за ореолом → снова съел в i за 150–500 | возврат | орбита внутри r ≤ 36; случайный натык |
| Два раза `(0,0,0)` в одном диске: раз ждал, раз ушёл | wait/leave | шум моторов |
| После кроссовера: часы innov A + тропизм B | сборка | оба гена от одного родителя |

`first_hidden_node`, `first_recurrent_cycle`, max generation, размер мозга —
комментарий *после* поведения, не определение успеха.

### 5.3 Контрольные рукава

Тот же мир, другой подарок:

1. сытость в сенсоре;
2. `leave_mode` в коде;
3. мир без джиттера центров.

Если «умное» поведение есть только там — канону не хватило памяти,
хватило желудка / флага / решётки. Это результат эксперимента, не дыра в теории.

Ложный друг: большой мозг при всё ещё чистом градиенте — толерантность к балласту,
не интеллект.

---

## 6. Порядок внедрения (когда трогать код)

1. Немарковский мир: патчи, скрытое `H`, убрать глобальный `FOOD_TARGET`, джиттер,
   урезать старт на диск, подкрутить цену пустыни пакетом.
2. Archive поведения (кромка, возврат) рядом с топологическими вехами.
3. Факультативный контактный кроссовер.
4. Контрольные рукава для сравнения.
5. Не раньше: гены тела, path integration, trapline, чужие тела в запахе,
   хищник без видимости в этом тике.

Мир без смешения растит либо бегуна, либо часы, редко оба.
Пол без ухода — секс сиблингов. Один канон: ген становится репликатором,
только когда мигрант донёс связь на чужую крошку.

---

## 7. Связь с текущим кодом (фаза 0)

Сейчас реализовано иное: равномерный респавн `binomial(FOOD_TARGET − n, 0.002)`
в любую точку карты; размножение — клон; `speciation.py` — заглушка;
тело (углы, радиусы, пороги) зашито; Archive пишет топологические firsts.
Этот канон — целевая модель эксперимента, не статус репозитория.
